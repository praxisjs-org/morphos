---
"@morphos/styles": minor
---

Initial release. Optional, opt-in CSS recipes — one file per component plus shared design tokens — for every component across `@morphos/inputs`, `@morphos/overlays`, `@morphos/layout`, and `@morphos/feedback`.
