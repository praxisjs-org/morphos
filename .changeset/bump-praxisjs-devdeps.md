---
"@morphos/core": patch
"@morphos/inputs": patch
"@morphos/overlays": patch
"@morphos/layout": patch
"@morphos/feedback": patch
"@morphos/icons": patch
---

Bump `@praxisjs/*` dev dependencies used for local development and testing (`core` to `^2.1.0`, `decorators` to `^1.6.1`, `jsx` to `^0.7.5`, `runtime` to `^0.6.0`, `vite-plugin` to `^4.0.5`). Peer dependency ranges are unchanged, so this doesn't affect what consumers can install.
